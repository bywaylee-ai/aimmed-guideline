# 1. 기본적인 설계 디스크립션은 상위 정책을 따른다.

## 1. 화면 구성시 제한

1. ** 기능 요구사항 외에 추가/변경 시 반드시 사용자의 승인 과정을 거친다. **
2. 

## 2. 폼 요소

### 2.1 텍스트 입력 필드

1. 텍스트 입력 필드 구조:
    
    !image.png
    
    !image.png
    
    1. 레이블 텍스트 : Label Text
    2. 인풋 텍스트 : Input Text
    3. 테일링 아이콘 : Tailing Icon
    4. 서포팅 텍스트 : Supporting Text
    5. 플레이스 홀더 : Place Holder
2. 인풋 텍스트 디폴트값 :  
    1. 모바일  : 글자수 기본사이즈 20글, Max 100글자
    2. PC, 웹화면 : 글자수 기본사이즈 20글자, Max 100글자
3. 플레이스 홀더
    1. 플레이스 홀더 추가한 경우 플레이스 홀더 사용
    2. 플레이스 홀더 없는 경우 해당항목의 제목 또는 레이블에 맞춰 “{레이블} 입력” 으로 자동으로 추가한다.
4. 텍스트 필드 상태 : 활성화(기본값), 비활성화, 에러, 포커싱, 호버
    
    !image.png
    
    1. 활성화(Enabled) : 기본값
    2. 마우스오버(Hovered) : 마우스 오버
    3. 포커스(Focused) : 포커싱, 또는 커서 위치, 입력중 가능한 상태
    4. 오류(Error, 에러)  : 입력 오류가 있는 경우, 서포팅 텍스트와 연계
    5. 비활성화(Disabled) : 비활성화, 사용안함 처리된 상태
5. 입력 오류 발생시 서포팅 텍스트에서 에러 표시
6. 입력 오류 발생시 폼을 저장하지 않거 에러표시 및 해당 에러에 포커싱

### 2.2 패스워드 입력 필드

1. 패스워드 입력폼은 텍스트 입력폼의 정책을 따른다.
2. 패스워드 최소 입력값 : 8자리 이상 20자리 이하
3. 패스워드 기본 규칙 : 영문+숫자+특수문자 조합으로 3가지 이상 만들어져 있는지 체크 필요
4. 패스워드 최초 생성시 2회 입력 : 패스워드 1회 입력값과 2회 입력값이 동일한지 체크 필요 
5. 패스워드는 db에 저장할때 암호화 알고리즘 사용 : ?? 어떤 암호화 방식 사용할 것인지는 DB 설계 에이전트가 판단한다.

### 2.3 로그인 버튼

1. 로그인 화면에서 사전 입력항목(아이디, 패스워드 등)이 완성되기전 로그인 버튼 비활성화
2. 로그인 사전입력 항목 : 아이디, 패스워드, 이름, 보안코드 등 상황에 따라 로그인 입력항목은 변경될 수 있음
3. 로그인 조건이 충족되면 로그인 버튼 활성화됨

## 3. 기능정의서 설명

#### 스프레드시트 또는 엑셀파일 화면설계서를 분석에서 페이지와 UI를 생성하는 규칙이 포함된 마크다운 문서

#### 3.1 기능정의서 기본 사용 규칙

1. 스프레드 시트의 행과 열을 잘 사용한다.
2. 행(Row)은 각각 항목을 기록한다.
3. 열(Colulm)은 반복되는 요소를 정리한다.
4. C열 분류는 현재 화면 또는 기능의 그룹등을 정의한다.
5. D열~G열은 메뉴의 상위하위 체계를 정의한다. 메뉴는 트리구조로 되어있다. 
6. H열 5Depth은 페이지 하의의 블록, 기능, 모듈 명 등을 정의한다.
7. I열은 화면명을 정의한다
8. J열 화면 종류 : 페이지(화면), 레이어팝업, 풀팝업, 바텀시트, 빈페이지 등이 있다.
9. **K열 표시내용(요소/Element) : 주요 UI 및 요소(Elememt) 항목들에 대한 내용이 정의되어있다.**
10. **L열 내용 : K열 요소에 대한 상세한 내용이 기술 되어 있다.**
11. M열 비고 : 기능적으로 추가되어야 하는 체크사항이나 항목이 기술 되어 있다.

#### 3.2 컬럼명

1. A열 : 번호,
2. B열 : ID
3. C열 : 분류
4. D열 : 1Depth 메뉴
5. E열 : 2 Depth 메뉴
6. F열 : 3 Depth 메뉴
7. G열 : 4 Depth 메뉴
8. H열 : 5 Depth 모듈명 또는 기능명, 블록 제목 등
9. I열 : 화면명(PageID)
10. J열 : 화면 종류
11. **K열 : 표시내용 요소 (Element)**   
12. **L열 :  내용은 K열에 대한 추가 기능 정의**
13. M열 : 비고

### 3.3 기능정의서 메뉴 구성

1. 기능정의서 기능정의 탭에서 1Depth~4Depth 까지 피벗 형태의 트리구조로 되어있다.
2. 화면 종류는 Page, 블록, 팝업, 레이어팝업, 빈페이지 등이 있다.
3. ** 화면 종류에서 블록, 버튼, 텍스트, 콘텐츠는 메뉴 구조에서 제외한다.**
4. 화면 Depth, 화면명, 화면종류를 구분하여 메뉴트리를 순서없는 목록(UL)형태로 마크업한다.
